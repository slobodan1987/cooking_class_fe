import { ReviewStatusPipe } from './review-status.pipe';

describe('ReviewStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
